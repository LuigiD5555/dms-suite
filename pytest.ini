[pytest]
# Do not set DJANGO_SETTINGS_MODULE. conftest.py configures Django directly
# with in-memory SQLite to avoid depending on Docker or MariaDB.
python_files = tests/test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    -v
